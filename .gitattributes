# Auto detect text files and perform LF normalization
* text=auto

# These files are consumed directly by Linux tools, including when a bundle is
# produced from a Windows checkout.
*.sh text eol=lf
*.service text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
Caddyfile* text eol=lf
